# 🚀 AutoReturn - Unified AI Intelligence Hub

<div align="center">

![AutoReturn Logo](https://img.shields.io/badge/AutoReturn-Orchestrator--Agent-red?style=for-the-badge&logo=ai&logoColor=white)

**A high-performance Unified Inbox powered by a custom Orchestrator-Agent architecture. Manage Gmail, Slack, and Local AI in one lightning-fast interface.**

[![Python](https://img.shields.io/badge/Python-3.10+-3776AB?style=flat-square&logo=python&logoColor=white)](https://python.org)
[![Architecture](https://img.shields.io/badge/Architecture-Orchestrator--Agent-blueviolet?style=flat-square)](https://github.com/kashan-saeed/AutoReturn)
[![Ollama](https://img.shields.io/badge/Ollama-Local%20LLM-black?style=flat-square&logo=ollama&logoColor=white)](https://ollama.ai)

</div>

---

## 🧠 The New Architecture

AutoReturn has moved away from traditional monolithic design to a **Decoupled Orchestrator-Agent Architecture**. This design (invented for maximum responsiveness) separates data fetching from AI intelligence.

### 🧩 Core Components

1. **The Orchestrator**: The central brain that routes natural language commands and manages the lifecycle of all agents.
2. **Intelligent Agents**: Specialized workers (GmailAgent, SlackAgent) that execute tasks concurrently using `asyncio`.
3. **Progressive Loading**: A revolutionary UI approach where messages are fetched instantly (<1s) and AI summaries are "layered" on top in the background using a non-blocking queue.

---

## ✨ Key Features

### 📨 Unified Inbox

* **Gmail & Slack Integration**: Real-time sync of emails and chat messages.
* **Instant Load**: Messages appear immediately after fetching; no waiting for AI.
* **Unified Search**: Search across both platforms with advanced filters.

### 🤖 AI Intelligence & Automation

* **Smart Summaries**: Automatic 1-2 sentence summaries generated by `kimi-k2.5` or `Llama3` using a parallelized non-blocking queue (up to 5 concurrent jobs).
* **Automation Policy Engine**: Per-message routing to `draft_only`, `plain_reply`, or `auto_reply` based on DND + allowlist settings.
* **DND-Aware Desktop Alerts**: When DND is ON, OS desktop notifications are suppressed.
* **AI Task Classification**: Every message is automatically classified into one of 5 actionable categories:
  * 📎 **File Attachment Required** — sender is requesting a document
  * ✍️ **Draft Generation** — needs a detailed, composed reply
  * ⚡ **Auto Reply** — transactional message, simple acknowledgement
  * 💬 **Simple Reply Required** — quick response or confirmation expected
  * ℹ️ **Informational** — no action needed, read-and-archive
* **4-Part Priority Algorithm**: A high-speed hybrid algorithm integrating:
  * **Algorithm 02 (Keyword Engine)**: scans for Direct Urgency, Time Pressure, and Action Calls with spaCy-powered negation detection.
  * **Algorithm 03 (Deadline Engine)**: Regex-based extraction of absolute (dates) and relative (today, tomorrow) deadlines with urgency bonuses.
  * **Algorithm 04 (Sender Engine)**: Tone-aware importance scoring using a configurable User Priority List.
* **Dynamic Rules Editor**: A fully functional Priority Rules editor in Settings allowing live updates to the AI's priority weights without restarts.
* **Auto-Reply Status Tracking**: Row-level state for pending/sent/failed with audit logging to `data/automation_audit.jsonl`.
* **Thread-Safe Gmail Auto Replies**: Gmail auto replies are sent under the original thread.
* **Attachment Resolver**: Uses user-allowed file/folder paths to suggest or auto-attach relevant files.
* **Plain Reply Pre-Send Review**: For plain-reply flow, review dialog shows final message + attachments before send.
* **Attachment Ambiguity Handling**: If multiple file candidates are found, user is prompted to select; if unresolved, send is blocked and composer reopens.

### 📅 Calendar & Event Extraction

* **Hybrid Detection**: Combines keyword/regex matching with LLM fallback for robust event detection.
* **Event Types**: Detects meetings, deadlines, appointments, and reminders from both Gmail and Slack messages.
* **ICS Export**: Extracted events can be reviewed and exported to `.ics` format for any calendar app.
* **Confidence Scoring**: Each detected event includes a confidence score for accuracy transparency.

### 🎭 Advanced Tone Detection

* **80% Accuracy**: Hybrid deterministic algorithm with lexicon + embedding fallback.
* **Real-time Analysis**: Sub-5ms processing for instant tone detection.
* **Smart Tone Suggestions**: Automatic tone recommendations based on message content and platform (Gmail vs Slack).

---

## 🆕 Latest Updates (March 2026)

* Added end-to-end automation policy routing: `draft_only`, `plain_reply`, `auto_reply`.
* Added DND rule to suppress desktop notifications while DND is enabled.
* Added plain-reply pre-send review dialog (`Send Now`, `Edit in Composer`, `Cancel`).
* Added attachment resolver from allowed local paths (`file_access_paths` + `max_auto_attachments`).
* Added ambiguity handling for attachments (prompt to choose when multiple candidates exist).
* Added blocking rule for unresolved required attachments (reopen composer for manual attach).
* Added Gmail same-thread auto reply behavior.
* Added automation audit logging (`data/automation_audit.jsonl`).
* Added row/status indicators for auto-reply outcomes and attachment-aware draft state.
* Added runtime status improvements (urgent count + automation status visibility updates).

---

## 📁 Project Structure

```text
AutoReturn/
├── main.py                  # Entry point
├── run.sh                   # Startup script
├── src/
│   ├── backend/
│   │   ├── core/
│   │   │   ├── orchestrator.py
│   │   │   ├── draft_manager.py
│   │   │   ├── tone_engine.py
│   │   │   ├── automation_coordinator.py
│   │   │   ├── reply_policy_engine.py
│   │   │   └── attachment_resolver.py
│   │   ├── agents/          # Intelligent Workers (Gmail, Slack)
│   │   ├── services/
│   │   │   ├── ai_service.py
│   │   │   ├── gmail_backend.py
│   │   │   ├── slack_backend.py
│   │   │   └── automation_settings_service.py
│   │   └── models/
│   │       └── automation_models.py
│   └── frontend/
│       ├── ui/
│       │   └── autoreturn_app.py
│       ├── dialogs/
│       │   ├── settings_dialog.py
│       │   ├── plain_reply_review_dialog.py
│       │   ├── send_gmail_reply_dialog.py
│       │   └── send_slack_message_dialog.py
│       └── widgets/         # UI components (ToneDetectionDisplay, ToneSelector)
├── config/                  # Settings & JSON storage (tone profiles, preferences)
├── data/
│   ├── automation_settings.json
│   └── automation_audit.jsonl
├── docs/                    # Technical documentation
│   ├── backend_architecture.md
│   └── Tone_Detection_Algorithm.md
└── logs/                    # Application logs
```

---

## 🔧 Technical Highlights

### 🎯 Tone Detection Engine

* **Hybrid Algorithm**: Combines 60+ word lexicon with spaCy embedding similarity

* **9-Stage Pipeline**: Preprocessing → Tokenization → Scoring → Normalization → Classification
* **Context Awareness**: Handles negation, intensifiers, and combined slurs
* **Performance**: <5ms processing time with 80% accuracy on real-world messages

### 🧠 Learning & Adaptation

* **User Preference Storage**: JSON-based tone profiles with sender/domain preferences

* **Real-time Learning**: Learns from manual tone selections in Gmail/Slack dialogs
* **Effectiveness Tracking**: Monitors tone quality and adapts recommendations
* **Auto-tone Suggestions**: Intelligent tone recommendations based on tone detection

### 🎨 UI/UX Integration

* **ToneDetectionDisplay Widget**: Shows detected tone with confidence indicators

* **ToneSelector Component**: Manual override with "Detected: [Tone]" display
* **Real-time Updates**: Immediate analysis as users compose messages
* **Platform Awareness**: Different defaults for Gmail vs Slack

---

## 🛠️ Setup & Installation

### 1. Prerequisites

* **Python 3.12+**
* **Ollama** (Download from [ollama.ai](https://ollama.ai))

### 2. Quick Start

```bash
# Clone the repository
git clone <repo-url>
cd AutoReturn

# Install dependencies
pip install -r requirements.txt

# Install spaCy model used for semantic analysis
python -m spacy download en_core_web_md

# Pull the required AI model
ollama pull kimi-k2.5:cloud

# Authenticate with Ollama Cloud (required for cloud models)
ollama signin

# Launch the app
./run.sh
```

---

## ⚙️ Configuration

The configuration is handled via the UI settings menu.

* **Gmail**: Requires `client_secret.json` from Google Cloud Console.
* **Slack**: Requires a User Token (`xoxp-...`) with `history` and `read` scopes.
* **Automation**:
  * `DND ON` + `Auto Reply OFF` → generate drafts only.
  * `DND ON` + `Auto Reply ON` + sender allowlisted → auto reply.
  * `DND OFF` → plain reply flow (with pre-send review).
  * `file_access_paths` + `max_auto_attachments` control attachment lookup.

---

## ✅ Quick Test Checklist (New Automation Features)

* Turn `DND ON` and verify desktop pop-up notifications stop.
* `DND ON` + `Auto Reply OFF`: receive new message and confirm draft is generated/surfaced in UI.
* `DND ON` + `Auto Reply ON` + allowlisted sender: confirm auto reply sent and row status updates.
* For Gmail auto-reply, confirm sent reply appears in the same thread.
* `DND OFF`: trigger plain reply and verify pre-send review dialog appears (`Send Now / Edit in Composer / Cancel`).
* Send a message that requests a file:
  * single clear match → file auto-attached/suggested,
  * multiple matches → selection prompt appears,
  * no match → send blocked with guidance to attach manually.
* Confirm audit records are appended to `data/automation_audit.jsonl`.

---

## 📈 Development Roadmap

* [x] Orchestrator-Agent Architecture

* [x] Progressive Loading (Fast Fetch)
* [x] Background AI Summarization (Parallel Processing)
* [x] Hybrid Tone Detection (80% accuracy)
* [x] **4-Part Priority Ranking Algorithm (Implemented)**
* [x] **Dynamic Priority Rules Editor (Functional)**
* [x] **AI Task Classification (5-Category System)**
* [x] **Calendar & Event Extraction (Hybrid Detection + ICS Export)**
* [x] User Preference Learning & Tone History
* [x] Real-time Tone Detection and Suggestions
* [ ] Universal Smart Draft generation
* [ ] Multi-language Support

---
<div align="center">
**Built as a Final Year Project at NUCES FAST Peshawar**
</div>
