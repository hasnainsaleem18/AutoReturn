# ==============================================
# AutoReturn - Unified Communication Management
# Python Dependencies
# ==============================================

# GUI Framework
PySide6>=6.5.0

# Slack Integration
slack-sdk>=3.21.0

# Gmail API Integration
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth>=2.23.0

# AI and ML (Used for tone detection and learning system)
# pydantic-ai>=0.0.13  # Full install may pull in heavier GPU-oriented dependencies on some setups
pydantic-ai-slim>=0.0.13  # Lighter option for CPU-only environments
pydantic>=2.0.0      # Used for tone models and learning data structures
ollama>=0.1.0

# CPU-focused note:
# Prefer pydantic-ai-slim on laptops without GPU acceleration needs.
# If you ever need the full stack later, install it manually outside this file.

# Desktop Notifications
plyer>=2.1.0

# Secure Credential Storage
keyring>=24.0.0

# Utilities
python-dotenv>=1.0.0
aiosqlite>=0.19.0      # Used for local data storage, also supports JSON-based learning system
supabase>=2.5.0

# Core NLP Processing (MUST use medium model for vectors)
spacy>=3.7.0

# Scientific Computing (Used in tone detection for vector operations)
numpy>=1.26.0,<2.0

# Voice control
numba==0.62.1
llvmlite==0.45.1
openai-whisper>=20231117
sounddevice>=0.4.6
keyboard>=0.13.5; platform_system != "Darwin"
pyobjc-framework-Quartz>=11.0; platform_system == "Darwin"
scipy>=1.11.0

#Linux System Dependencies for sounddevice and audio input/output functionality
# sudo apt-get install libportaudio2 libportaudiocpp0 portaudio19-dev python3-pyaudio

# Data Processing (Used for tone detection data structures)
pandas>=2.0.0

# Event extraction and calendar export
dateparser>=1.2.0
tzlocal>=5.2


# Optional: For enhanced performance
# spacy-transformers>=1.2.0  # For transformer-based models
# en-core-web-lg>=3.7.0      # Larger English model (more accurate, slower)
# en-core-web-trf>=3.7.0    # Transformer-based model (most accurate, slowest)
